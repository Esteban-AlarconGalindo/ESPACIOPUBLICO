#map { height: 500px; }

.textImg{

    position: relative;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.textLarge {
    font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
    font-size: 10em;
    font-weight: bold;
}

.textMedium {
    
    font-size: 3em;
    
}